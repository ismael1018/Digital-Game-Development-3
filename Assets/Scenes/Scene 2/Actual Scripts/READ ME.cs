using System.Diagnostics.CodeAnalysis;
using UnityEngine;

public class README : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    //what went well?
    // what went well was being able to show the dice able to move when rolling it, the camera view.


    // what didn't went well?
    // I wasn't sure how to code it so where you would roll and then move without gettung hurt or healing on the tiles so i had to cheese it where the player
    // would has to float to whatever he lands on. i wasn't also sure how to rotate the cammera view if i wanted the player to make a left or right turn so-
    // i just did it in a staight line which kinda suck. 

    //What challenges did i face?
    //The challenge that I face are written above but one challenge i did do well one was the dice roll, i did want to make it so the player can make a turn-
    // instead of it being in a straight line. i think that would've made the game more appealing. 

    //What can you do differently?
    // figure out the camera view switch or rotation so the game looks more appealing and more interesting instead of it just being a straigh line.
    // maybe other cool tiles that would make this game more interesting and fun
}
